/**
 * Retired generator — Competitor Wall is owned by generate-vma-site.mjs.
 * npm run generate:competitors → generate:vma
 */
console.log('generate-competitor-wall.mjs is retired. Use: npm run generate:vma');
process.exit(0);
