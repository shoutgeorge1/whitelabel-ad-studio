/**
 * Retired generator — Ad Production owns studio.html.
 * npm run generate:studio → generate:vma
 */
console.log('generate-studio-home.mjs is retired. Use: npm run generate:vma');
process.exit(0);
